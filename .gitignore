# Credentials and personal data. Group rosters (.env.chimgee, .env.friends)
# are deliberately NOT in the repo — they hold real names and addresses. The
# scheduled job restores them from GROUP_* secrets at run time.
.env
.env.*
!.env*.example

.venv/
__pycache__/
*.pyc

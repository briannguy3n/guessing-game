# Credentials only. Group rosters (.env.chimgee, .env.friends) are committed
# on purpose — they hold names and emails, no secrets, and the scheduled job
# needs them. Keep this repo private.
.env

.venv/
__pycache__/
*.pyc
